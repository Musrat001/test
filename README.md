# Full Stack Form Page

## Run locally

```bash
npm install
npm start
```

Then open `http://localhost:3000`.

## Deploy

This app is deployment-ready for Node.js platforms (for example Render, Railway, or Fly.io) using:

- **Build command:** `npm install`
- **Start command:** `npm start`